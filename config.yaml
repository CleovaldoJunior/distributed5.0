# config.yaml
detectors:
- name: detector regex
  keywords:
  - PASSWORD
  - TOKEN
  - PASS
  regex:
    adjective: (\S+)PASSWORD(\S+)
  regexToken:
    adjective: (\S+)TOKEN(\S+)
  regexPass:
    adjective: (\S+)PASS(\S+)
